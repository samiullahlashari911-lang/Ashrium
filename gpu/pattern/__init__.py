"""GarmentCode / PyGarment MIT 2D pattern ingest (task=pattern).

Never NvidiaWarp-GarmentCode. Pattern instantiate only — no Warp sim from that
fork. Style comes from HTML/product text parse, not Qwen3-VL.
"""
